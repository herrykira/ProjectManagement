<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tinku.projectmanagementsystem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.UserActivity" />
        <activity android:name=".ui.AdminLoginActivity" />
        <activity android:name=".ui.AdminActivity" />
        <activity android:name=".fragment.fragmentsforfab.ActivityForFloatingActionBar" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="0dbe0d6a7a7b06ee132b731786f597b8400ebc99" />
    </application>

</manifest>